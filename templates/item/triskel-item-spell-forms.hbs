<section
  class="tab item-panel {{tabs.spells.id}} {{tabs.spells.cssClass}} {{#if isSpellType}}is-hidden{{/if}}"
  data-tab="{{tabs.spells.id}}"
  data-group="{{tabs.spells.group}}"
>
  <h2>Attunements</h2>
  <ol class="reference-entries">
    {{#each references.spellForms as |reference|}}
      <li class="reference-row">
        <span class="reference-label">{{reference.label}}</span>
        <button type="button" data-action="removeSpellFormReference" data-index="{{reference.index}}">Entfernen</button>
      </li>
    {{/each}}
    {{#unless references.spellForms.length}}
      <li class="reference-row reference-row--empty">Keine Referenzen hinterlegt</li>
    {{/unless}}
  </ol>

  <div class="reference-controls__inputs">
    <select
      id="spell-forms-reference-select"
      name="spellFormsRef"
      data-reference-select="spellForms"
    >
      {{#each referenceOptions.spellForms as |option|}}
        <option value="{{option.value}}">{{option.label}}</option>
      {{/each}}
    </select>

    <button
      type="button"
      data-action="addSpellFormReference"
      data-select-field="spellFormsRef"
      data-ref-path="system.spellForms.ref"
    >
      Hinzufügen
    </button>
  </div>
</section>
